# my-secretary（自分専用の秘書）の作法

このプラグインは、非エンジニアの受講生が Claude Code デスクトップアプリで
「自分専用のAI秘書」を持つための教材プラグイン。コマンドとスキルはこの作法を前提にする。

**テーマは「使えば使うほど、自分専用の秘書として育つ」**。記録（メモ・タスク・日報）の
蓄積と、学び（好み・訂正・パターン→ `秘書の記憶.md`）の蓄積が、出迎え・段取り・応対の質に
還元される設計を最優先にする。新機能はこのループに乗るかで判断する。

## 設計原則

- **既定はローカル Markdown だけで完結**。受講生の環境差・認証つまずきを持ち込まないため、
  **連携前提の機能は作らない**（コネクタが無くても全機能が動くこと）。
  Gmail / Google カレンダーのコネクタは**任意の上乗せ**として扱い、各スキルの末尾に
  「接続されている場合（任意）」節で書く。外向きの操作（カレンダー登録・下書き保存）は
  **毎回確認してから**。**送信・投稿は絶対にしない**。詳細は `docs/連携ガイド_GmailとGoogleカレンダー.md`。
- **秘書の人格は執務室の `CLAUDE.md` に置く**。フォルダを開くだけで秘書が出迎える体験が核。
  1行目のマーカー `<!-- my-secretary:workspace v1 -->` を執務室の目印にする（変えない）。
- **自発的な声かけは UserPromptSubmit フック**（`scripts/user_prompt.py`）。ユーザーの発言ごとに
  走り、「いま伝える価値がある」ときだけ `【秘書の気づき】` を1〜2行注入する
  （予定45分前／夕方に日報なし／今日締切が残る／返事待ち3日超／打ち合わせ後の記録促し）。
  **1セッション1話題1回**（状態は OS の一時領域。執務室は汚さない）、**先回り度に従う**、
  執務室以外では完全に沈黙、毎回走るので軽量（実測 約30ms）。
  ※ セッション外からの通知は原理的に不可（CronCreate はセッション限定・7日で失効、
  SessionEnd フックは発話できない）。「開いている間は気が利く」が到達点。
- **出迎えは SessionStart フックで補強**（`hooks/hooks.json` → `scripts/session_start.py`）。
  起動時に執務室を検出し、状態スナップショット（時刻・タスク・日報・受信箱）と
  「まず秘書として出迎えよ」をコンテキスト注入する。これで最初のひとことが何であっても
  秘書モードになる。**執務室以外では必ず無出力**（他プロジェクトを汚さない）、
  エラー時も黙って exit 0（セッション開始を邪魔しない）。resume では動かない。
  なお「入力ゼロで秘書が先に発話」はデスクトップアプリでは不可（`initialUserMessage` は
  `claude -p` 専用）— 最初の1メッセージは必要、という前提で設計する。
- **既存ファイルは上書きしない**。追記・新規作成・チェックボックス更新・受信箱整理の転記のみ。
  例外は秘書が管理する2ファイル: `申し送り.md`（最新状態に書き直す）と `秘書の記憶.md`
  （追記自由・1行報告、「忘れて」で行削除、たまの整理可）。
  加えて**本人が育てる3種**（`業務手順/`・`朝会・夕会の項目.md`・`繰り返し.md`）は
  **本人の指示があれば書き換えてよい**（消すときは1回確認・消した内容は記録に残す）。
  秘書がユーザーのファイルを**削除することは絶対にない**（作り直しでもデータフォルダは残す）。
- **所有権の区分**: プロフィール.md＝本人の基本情報と目標（**提案→承認**で更新）／
  秘書の記憶.md＝秘書の学び（自由に育てる）／申し送り.md＝セッション引き継ぎ（書き直し）。
  学びは即座にふるまいへ反映し、**同じ指摘を二度受けない**。成長は見える化する
  （記憶件数・勤続日数・蓄積件数を出迎えとダッシュボードに出す）。
- **執務室限定スキル13点**（appointments＝予定／deadline-watch＝締切・返事待ち／
  recall＝思い出し／handover＝申し送り／learning＝学習・記憶／people＝人物・打ち合わせ準備／
  drafting＝代筆／**routines＝定型業務レシピ／customize＝自分仕様（朝会・夕会の項目・領域）／
  recurring＝繰り返し／estimates＝見積もりと実績／intake＝議事録・画像の取り込み**〔v1.3.0〕／
  **decisions＝決めたこと**〔v1.4.0〕）は、
  リポジトリの `workspace-skills/` が正。setup（`init_workspace.py --plugin-root`）が執務室の
  `.claude/skills/` へ複製する（既存は上書きしない＝ユーザーの育てた版を尊重）。
  プラグイン更新では自動更新されないので、**既存ユーザーの移行経路は `/my-secretary:update`**
  （本体更新の案内 → init_workspace で不足スキル補充 → CLAUDE.md への行追記を承認制で）。
  doctor は不足を検出したら update へ誘導する。**機能を足したら必ず update の導線を確認する**。
  締切表記は `（締切: YYYY-MM-DD）` の1形式のみ。recall は原文引用＋出典必須（創作禁止）。
- **冪等**。`init_workspace.py` は何度実行しても安全（既存はスキップして報告）。
- **日付は必ず `date` コマンドで取得**（モデルの推測は日付を間違えるため）。
- **書式の唯一の正は `skills/secretary-mode/references/templates.md`**。
  コマンド側に書式をコピーして二重管理しない。
- **ヒアリングは1問ずつ、確認は1回だけ**。質問攻めはワークショップのテンポを壊す。
- **提案は控えめに、頻度は本人が決める**（キャラ設定の「先回り度」）。断られた提案は
  同じセッションで繰り返さない。研究上も過剰な先回りは信頼と自律性を損なう
  （根拠: `docs/リサーチ_秘書に求められる機能.md`）。
- **役割の型（v1.3.0）は「質問を増やさず、提案する」**。setup の質問1（お仕事）から秘書が近い型を
  選んで Step 3 の確認に含める。型が変えるのは 追加フォルダ／朝会・夕会で聞くこと／気をつけること／
  定型業務の候補／記憶の初期値 の5つだけ（名前・口調・記録には触れない）。唯一の正は
  `skills/secretary-mode/references/role-presets.md`、決め直しは `/my-secretary:role`。
  update では型を勝手に当てない（案内だけ）。
- **繰り返し（v1.3.0）は `繰り返し.md` の行書式が契約**（`- 毎週 月 10:00 内容`）。
  読むのは `scripts/recurring.py` ただ1つ（session_start / user_prompt / doctor が共用）。
  書式を増やすときは recurring.py とスキルとテンプレートを同時に直す。書式で書けない周期は
  `予定.md` に単発で（書式を複雑にしない）。
- **朝会・夕会の追加項目（役割・自分仕様・見積もりの実績）があっても、質問のメッセージは1つのまま**
  （同じメッセージに短く並べる）。「1問ずつ」の原則を崩さない。
- **声かけの拡張（週初めの締切まとめ・棚卸し・記念日・月末・静かな時間帯）は未着手**。
  Google カレンダー連携の作り込みと一緒にやる（2026-09-07 決定）。
- **続けやすさ（v1.4.0）**: 「ぜんぶ投げる」（intake C）は**1行も落とさない**（迷ったら受信箱）・元の文を書き換えない・
  承認1回。朝会のショート版（morning 手順0）は**質問ゼロ・1メッセージ**で、秘書が★を決める（「朝会は短め」の記憶で既定化）。
  決めたこと（`決めたこと.md`）は**行を消さない**（撤回は追記）。「なぜ」の問いは recall より先に決めたことを見る。
- **育ちが見える（v1.4.0）**: 月次の成長カードは**ファイルから数えた数字だけ**（数えられない項目は「—」。誇張・言い換えをしない）。
  記憶の棚卸しは**20件以上のときだけ・最大5件・承認1回**（承認なしに消さない）。代筆の型 `下書き/_型.md` は
  **同じ直しが2回**入ってから足す（1回で決めつけない）。相手の情報は型に入れない。
- **キャラは強制しない**。setup で決めるのは名前・呼び名だけ。性別・性格・口調は標準キャラ
  （決めない × しっかり者 × あたたかい秘書）で開始し、`/my-secretary:character` で
  決めたいときに決める。プリセットと標準キャラの唯一の正は
  `skills/secretary-mode/references/persona-presets.md`。
- **挨拶が入口**。朝の始まりの合図→朝会、1日の終わりの合図→夕会（17時以降・日報未作成のとき）へ
  挨拶の返事と同じメッセージで自然につなげる。時間帯は `date` で確かめる。
  **言葉は意味で判定**（「おはよう」「おつかれさま」は例。「おはよー」「今日終わり！」等も同じ扱い）。
  提案は1回に1つ、断られたら同じセッションでは繰り返さない。
- Python は `python3` → `python` → 手動生成の順でフォールバック（Windows 対応）。

## 文体

- ユーザー向けの文はやさしい日本語。カタカナ専門用語を避ける
  （ワークスペース→執務室／キャプチャ→書き留める）。
- 秘書としての応答は、執務室 `CLAUDE.md` の口調プリセットに常に従う。
  記録系の応答は「実行 → 保存先1行 → 口調のひとこと」で短く。
- 責めない。未完タスクが多い日も「持ち越しは明日拾う」で安心させる。

## 成果物（執務室に生成されるもの）

- `CLAUDE.md`（秘書の人格。references/claude-md-template.md から生成）
- `プロフィール.md`（ユーザー情報。秘書が育てる）
- `受信箱/・タスク/・アイデア/・ノート/・日報/・週次レビュー/・月次レビュー/`（標準7フォルダ＋追加領域）
- 必要になったとき作られるもの: `予定.md`・`繰り返し.md`・`朝会・夕会の項目.md`・`決めたこと.md`・`人物/`・
  `下書き/`（と `下書き/_型.md`）・`議事録/`・`業務手順/`
- 日付ファイルは `YYYY-MM-DD.md`、週次は `YYYY-Www.md`、月次は `YYYY-MM.md`

## リリース手順

1. 動作確認: `python3 scripts/init_workspace.py` と `scripts/doctor.py` を一時フォルダで実行
2. `.claude-plugin/plugin.json` の version を上げる（marketplace.json は version を持たない）
3. commit & push（GitHub: miyazakiryuji/my-secretary）
4. 配布 zip が必要なら `bash scripts/make_dist.sh`
