# my-secretary（自分専用の秘書）の作法

このプラグインは、非エンジニアの受講生が Claude Code デスクトップアプリで
「自分専用のAI秘書」を持つための教材プラグイン。コマンドとスキルはこの作法を前提にする。

**テーマは「使えば使うほど、自分専用の秘書として育つ」**。記録（メモ・タスク・日報）の
蓄積と、学び（好み・訂正・パターン→ `秘書の記憶.md`）の蓄積が、出迎え・段取り・応対の質に
還元される設計を最優先にする。新機能はこのループに乗るかで判断する。

## 設計原則

- **ローカル Markdown だけで完結**。外部サービス連携（カレンダー・メール等）は入れない。
  受講生の環境差・認証つまずきを持ち込まないため。
- **秘書の人格は執務室の `CLAUDE.md` に置く**。フォルダを開くだけで秘書が出迎える体験が核。
  1行目のマーカー `<!-- my-secretary:workspace v1 -->` を執務室の目印にする（変えない）。
- **出迎えは SessionStart フックで補強**（`hooks/hooks.json` → `scripts/session_start.py`）。
  起動時に執務室を検出し、状態スナップショット（時刻・タスク・日報・受信箱）と
  「まず秘書として出迎えよ」をコンテキスト注入する。これで最初のひとことが何であっても
  秘書モードになる。**執務室以外では必ず無出力**（他プロジェクトを汚さない）、
  エラー時も黙って exit 0（セッション開始を邪魔しない）。resume では動かない。
  なお「入力ゼロで秘書が先に発話」はデスクトップアプリでは不可（`initialUserMessage` は
  `claude -p` 専用）— 最初の1メッセージは必要、という前提で設計する。
- **既存ファイルは上書きしない**。追記・新規作成・チェックボックス更新・受信箱整理の転記のみ。
  例外は秘書が管理する2ファイル: `申し送り.md`（最新状態に書き直す）と `秘書の記憶.md`
  （追記自由・1行報告、「忘れて」で行削除、たまの整理可）。
  秘書がユーザーのファイルを**削除することは絶対にない**（作り直しでもデータフォルダは残す）。
- **所有権の区分**: プロフィール.md＝本人の基本情報と目標（**提案→承認**で更新）／
  秘書の記憶.md＝秘書の学び（自由に育てる）／申し送り.md＝セッション引き継ぎ（書き直し）。
  学びは即座にふるまいへ反映し、**同じ指摘を二度受けない**。成長は見える化する
  （記憶件数・勤続日数・蓄積件数を出迎えとダッシュボードに出す）。
- **執務室限定スキル**（deadline-watch＝締切／recall＝思い出し／handover＝申し送り／
  learning＝学習・記憶）は、
  リポジトリの `workspace-skills/` が正。setup（`init_workspace.py --plugin-root`）が執務室の
  `.claude/skills/` へ複製する（既存は上書きしない＝ユーザーの育てた版を尊重）。
  プラグイン更新では自動更新されないので、doctor が不足を検出したら setup 再実行で補充する。
  締切表記は `（締切: YYYY-MM-DD）` の1形式のみ。recall は原文引用＋出典必須（創作禁止）。
- **冪等**。`init_workspace.py` は何度実行しても安全（既存はスキップして報告）。
- **日付は必ず `date` コマンドで取得**（モデルの推測は日付を間違えるため）。
- **書式の唯一の正は `skills/secretary-mode/references/templates.md`**。
  コマンド側に書式をコピーして二重管理しない。
- **ヒアリングは1問ずつ、確認は1回だけ**。質問攻めはワークショップのテンポを壊す。
- **キャラは強制しない**。setup で決めるのは名前・呼び名だけ。性別・性格・口調は標準キャラ
  （決めない × しっかり者 × あたたかい秘書）で開始し、`/my-secretary:character` で
  決めたいときに決める。プリセットと標準キャラの唯一の正は
  `skills/secretary-mode/references/persona-presets.md`。
- **挨拶が入口**。朝の始まりの合図→朝会、1日の終わりの合図→夕会（17時以降・日報未作成のとき）へ
  挨拶の返事と同じメッセージで自然につなげる。時間帯は `date` で確かめる。
  **言葉は意味で判定**（「おはよう」「おつかれさま」は例。「おはよー」「今日終わり！」等も同じ扱い）。
  提案は1回に1つ、断られたら同じセッションでは繰り返さない。
- Python は `python3` → `python` → 手動生成の順でフォールバック（Windows 対応）。

## 文体

- ユーザー向けの文はやさしい日本語。カタカナ専門用語を避ける
  （ワークスペース→執務室／キャプチャ→書き留める）。
- 秘書としての応答は、執務室 `CLAUDE.md` の口調プリセットに常に従う。
  記録系の応答は「実行 → 保存先1行 → 口調のひとこと」で短く。
- 責めない。未完タスクが多い日も「持ち越しは明日拾う」で安心させる。

## 成果物（執務室に生成されるもの）

- `CLAUDE.md`（秘書の人格。references/claude-md-template.md から生成）
- `プロフィール.md`（ユーザー情報。秘書が育てる）
- `受信箱/・タスク/・アイデア/・ノート/・日報/・週次レビュー/`（標準6フォルダ＋追加領域）
- 日付ファイルは `YYYY-MM-DD.md`、週次は `YYYY-Www.md`

## リリース手順

1. 動作確認: `python3 scripts/init_workspace.py` と `scripts/doctor.py` を一時フォルダで実行
2. `.claude-plugin/plugin.json` の version を上げる（marketplace.json は version を持たない）
3. commit & push（GitHub: miyazakiryuji/my-secretary）
4. 配布 zip が必要なら `bash scripts/make_dist.sh`
