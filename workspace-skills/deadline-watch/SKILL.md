---
name: deadline-watch
description: 締切・期日と「返事待ち」の管理。「8/15までに◯◯」「金曜までに」など期日つきの頼みごと、「今週の締切は？」「期限切れある？」という確認、締切の変更に使う。あわせて「◯◯さんに送った、返事待ち」「返事きてないのある？」など、相手のボールになっている案件の督促管理にも使う。この執務室（秘書フォルダ）専用。
---

# 締切ウォッチ（deadline-watch）

この執務室の秘書として、`CLAUDE.md` の人格・口調どおりに応対する。
日付は必ず Bash の `date` で解決する（推測しない）。

扱うのは2種類。**自分のボール＝締切**と、**相手のボール＝返事待ち**。

## 締切の書き方（唯一の形式）

タスク行の末尾に `（締切: YYYY-MM-DD）` を付ける:

```
- [ ] 請求書を送る（締切: 2026-08-15）
```

「金曜まで」「月末」「来週頭」などの相対表現は、`date` で実日付にしてから記録し、
「金曜＝08-01 ですね」とひとこと確認を添える（言い直されたらすぐ直す）。

## 操作

### 1. 締切つきタスクの記録（「◯日までに□□」）

今日のタスクファイル（`タスク/（今日）.md`）に、締切タグ付きで追記する。
締切が先の日付でも今日のファイルに書いてよい（下のスキャンがずっと拾い続ける）。

### 2. 締切スキャン（「今週の締切は？」「期限切れある？」）

1. `タスク/*.md` の**全ファイル**から、未完（`- [ ]`）かつ `（締切:` を含む行を集める（Grep可）
2. 同じ内容のタスクは1つにまとめる（どこかで `- [x]` になっていたら完了扱い）
3. 分類して表示: 🔴 期限切れ ／ 🟠 今日 ／ 🟡 3日以内 ／ ⚪ それ以降
4. 期限切れがあれば「今日やりますか？日付を変えますか？やめますか？」を**1回だけ**聞く

### 3. 締切の変更・取り消し

「◯◯の締切を△△に」→ 該当行の日付を書き換える。「締切なしで」→ タグを外す。
どの行か曖昧なら候補を見せて確認する。

### 4. 返事待ちの記録（「◯◯さんに送った、返事待ち」）

相手にボールが渡っていて、こちらは待つだけの案件。タスク行の末尾に付ける:

```
- [ ] 見積もりの返事（返事待ち: 田中さん / 2026-07-28から）
```

- 日付は**投げた日**（`date` で実日付に）。
- 「返事きた」「返信あった」と言われたら `- [x]` にするか、待ちタグを外して次の行動に書き換える。

### 5. 返事待ちスキャン（「返事きてないのある？」「督促いる？」）

1. `タスク/*.md` の全ファイルから、未完かつ `（返事待ち:` を含む行を集める（Grep可・重複は1つに）。
2. 投げた日からの経過日数を出し、**3日以上**のものを「そろそろ確認どき」として上に出す。
3. 表示例:

```
⏳ 返事待ち
- 見積もりの返事（田中さん・6日経過）← そろそろ確認どき
- 素材の受け取り（佐藤さん・1日経過）
```

4. 3日以上のものがあれば、「催促の文面を作りますか？」を**1回だけ**添える
   （頼まれたら drafting スキルへ）。

## 心得

- 締切は**本人の言葉に期日があるときだけ**付ける（勝手に設定しない）
- 朝会・ダッシュボード・セッション開始の出迎えでも、期限切れ・今日締切は最優先で知らせる
  （ただし提案は1回に1つのルールは守る）
- 返事待ちは**相手を責める書き方をしない**（「まだ来ていません」であって「遅い」ではない）
- 記録・変更したら保存先とあわせて1行で報告する
